# ── Virtual environment (1GB, machine-specific — recreate from requirements.txt) ──
sales_env/
venv/
.venv/

# ── Real business + customer data (PII: phone numbers, IPs, user IDs) — DO NOT publish ──
database/
*.parquet
*.xlsx
*.xls
*.csv
*.db
*.sqlite
*.sqlite3
*.bak
~$*          # Excel/Word lock & temp files

# ── Local config & secrets (machine-specific, never publish) ──
.env
.env.*
local_config.py
secrets.toml

# ── Generated / cached artifacts ──
static/
__pycache__/
*.py[cod]
*$py.class
.ipynb_checkpoints/

# ── Logs & runtime output ──
*.log
dashboard_stderr.log
dashboard_stdout.log

# ── Editor / OS ──
.vscode/
.idea/
.DS_Store
Thumbs.db

# ── Generated documentation export (regenerate via generate_doc_pdf.py) ──
Sales_Dashboard_Documentation.pdf
